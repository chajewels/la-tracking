# Cha Jewels Handover Handbook — Session 2026-05-25

**Date:** 2026-05-25 EOD | **Board:** 4 deferred items resolved, tier_changed completed

---

## SOP — LOCKED, NON-NEGOTIABLE

**Deployment Model:**
- FRONTEND → Firebase (auto-deploy on main push)
- EDGE FUNCTIONS → Lovable IDE ONLY
- DATABASE → Supabase SQL Editor (pure SQL)
- CODE AUDIT → Cloud Shell (git/grep only)

**Git Workflow:** Push directly to main (no feature branches). Every commit triggers auto-deploys.

**6-Step Cycle (LOCKED):**
1. Investigation (verbatim file state, line numbers)
2. Analysis (exact string replacements)
3. User Confirmation (explicit go-ahead)
4. Implementation (write code/SQL)
5. Ship (deploy via Lovable/SQL Editor)
6. Verification + Docs (test + update docs)

**SQL Rules:**
- NO placeholders (<account_id>, <EMAIL>)
- Self-resolving only (subquery/CTE)
- Pure SQL (no TypeScript)
- Run one at a time
- Enum casting: 'active'::account_status
- UUID: DISTINCT ON ... ORDER BY (no MIN/MAX)
- Table-alias all id references

**Lovable Prompts:**
- ONE code block (entire prompt copy-paste)
- Bundle code + CLAUDE.md cleanup
- Explicit: "Push directly to main"
- Verify against git state

**Cloud Shell:**
- Always: git pull origin main before verify (drifts with Lovable)
- On package-lock conflicts: git checkout -- package-lock.json
- Greps: <40 chars, single-line
- NEVER deploy from here

---

## SYSTEM STATE (2026-05-25)

**Board:**
- P3: Loyalty staff visibility → CLOSED (2026-05-24)
- P5: Session timeout 2hr → PENDING
- P6: Admin audit log → PENDING
- riskFactor dead code → OPTIONAL cleanup

**This Session:**
1. customer-statement deletion (7 files, 789 lines) — Commit 7f38d37
2. 4 Deferred items resolved:
   - Bug #103: send-transactional-email auto-deploy (already working)
   - RLS file 6: Phase B RLS policies (deployed via migrations 05/06)
   - tier_changed downgrade: COMPLETED
   - Documented: FIXED-BUGS.md #153–156

3. tier_changed Downgrade (Commit 0272587 + da0b6fc):
   - Expiry path (L364–383): tier_changed when member expires
   - Gap path (L477–496): tier_changed when purchase gap > threshold
   - Both non-blocking, match upgrade path structure
   - Member ledger complete for all tier events

---

## LOCKED RULES

**Calculation (NON-NEGOTIABLE):**
- totalLAAmount = total_amount + Σ(non-waived penalties)
- remainingBalance = totalLAAmount - Σ(non-voided payments)
- Penalty: 'unpaid' → counts, 'paid' → counts, 'waived' → EXCLUDED

**Currency (NON-NEGOTIABLE):**
- JPY = PHP ÷ php_jpy_rate (DIVIDE, not multiply)
- PHP = JPY × php_jpy_rate (MULTIPLY, not divide)
- Example (rate 0.42): ₱10,000 ÷ 0.42 = ¥23,810 ✓

**Schema (LOCKED):**
- customers: id, customer_code, full_name, email, mobile_number, auth_user_id (NO first_name/last_name)
- layaway_accounts: NO statement_token (deleted 2026-05-25)
- loyalty_members: NO tier column
- payments: NO payment_type (DP: ref LIKE 'DP-%')
- penalty_fees: 'unpaid' | 'paid' | 'waived' (NEVER 'active')

**Enums (EXACT):**
- account_status: active | overdue | completed | cancelled | forfeited | final_forfeited | extension_active | reactivated | final_settlement
- schedule_status: pending | partially_paid | paid | overdue | cancelled
- penalty_fee_status: unpaid | paid | waived
- loyalty_transaction_type: earned | bonus | redeemed | expired | adjusted | refunded | revoked | enrolled | tier_changed | ...

---

## NEXT STEPS

- Read CLAUDE.md + HANDOVER
- git pull origin main
- Check P5 session timeout, P6 admin audit log
- Review docs/OPEN-BUGS.md
- Confirm Lovable + Cloud Shell access

---

**Board:** Clean. 4 deferred items resolved. tier_changed completed. Ready for next priority.
